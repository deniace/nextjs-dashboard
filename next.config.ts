import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    // output: "export", // ini buat static export,
    // ppr: "incremental", // config ini buat Partial Prerendering, jadi gabungan antara static rendering dan dinamic rendering, .
};

export default nextConfig;
